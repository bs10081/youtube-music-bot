diff --git a/src/services/nowPlayingProvider.ts b/src/services/nowPlayingProvider.ts
index cfed6b2..7291bac 100644
--- a/src/services/nowPlayingProvider.ts
+++ b/src/services/nowPlayingProvider.ts
@@ -1,6 +1,8 @@
 import type { NowPlayingConnectionStatus, NowPlayingLyricPayload, NowPlayingTrackSnapshot } from '../types';
 
-export const NOW_PLAYING_WS_URL = 'ws://localhost:9863/api/ws/lyric';
+const NOW_PLAYING_WS_PROTOCOL = typeof window !== 'undefined' && window.location.protocol === 'https:' ? 'wss' : 'ws';
+const NOW_PLAYING_WS_HOST = typeof window !== 'undefined' && window.location.host ? window.location.host : 'localhost:9863';
+export const NOW_PLAYING_WS_URL = `${NOW_PLAYING_WS_PROTOCOL}://${NOW_PLAYING_WS_HOST}/api/ws/lyric`;
 const NOW_PLAYING_RECONNECT_DELAY_MS = 2000;
 
 type NowPlayingTrackMessage = {
diff --git a/src/utils/nowPlayingClock.ts b/src/utils/nowPlayingClock.ts
index 0f6ac4f..f290681 100644
--- a/src/utils/nowPlayingClock.ts
+++ b/src/utils/nowPlayingClock.ts
@@ -2,7 +2,7 @@ import type { NowPlayingLyricPayload, NowPlayingTrackSnapshot } from '../types';
 
 // src/utils/nowPlayingClock.ts
 
-export const NOW_PLAYING_PROGRESS_QUERY_URL = 'http://localhost:9863/api/query/progress';
+export const NOW_PLAYING_PROGRESS_QUERY_URL = '/api/query/progress';
 export const NOW_PLAYING_PROGRESS_POLL_INTERVAL_MS = 7000;
 export const NOW_PLAYING_PROGRESS_CORRECTION_THRESHOLD_SEC = 0.2;
 
